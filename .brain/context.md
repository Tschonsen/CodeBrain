# CodeBrain — project context

_Generated by codebrain-init 2026-04-19T19:44:46Z_

## Overview

CodeBrain is an **MCP server** that lets Claude Code offload bulk work to a local LLM (Ollama + qwen2.5-coder:14b). It is **not** an ML/AI project — CodeBrain itself is Python glue: an HTTP client for Ollama, a FastMCP stdio server, a per-file `.brain` scanner, and a verifier layer. The LLM is the tool, not the subject.

## Stack

- Detected: python
- Marker files: `pyproject.toml`
- Source files total: 10

## Top extensions

- `.py` — 10 files

## Notes for Claude

- **Phases 1–4 complete**, Phase 5 (RAG) deferred. State lives in `.spec/CURRENT.md` — read that before substantial changes.
- **Brain-file format spec**: `.spec/brain-file-format.md`. Programmatic frontmatter (scanner writes it deterministically), Qwen only writes the five sections.
- **Model limit**: qwen2.5-coder:14b ignores format-rules unreliably. Defense-in-depth is the design: fence-strip, skip-empty-sources, section validation, retry-with-tightened-instruction, verifier loop.
- **Tests are pflicht-grün**: 96 currently, run `.venv/Scripts/python -m pytest tests/` before any commit.
- **English in code, German fine in conversation.** Code artefacts (variables, functions, classes, commit messages, logs) are English. Domain terms without good translation may stay German.
- **Scope discipline**: don't mock databases in tests, don't add backwards-compat shims for removed code, no feature flags.
- **MCP tool timeout**: `scan_repo` on ~5+ files at once can exceed client deadline. Call on smaller scopes until progress-streaming lands (not on roadmap).
- **Hardware context**: RTX 4070 12 GB fits qwen2.5-coder:14b at Q5. Model bump to 32B needs second GPU (spare 4070 available on secondary machine — not wired up yet).

